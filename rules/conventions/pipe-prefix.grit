language js

`@Pipe({ $args })` where {
  $args <: contains `name: $pipeName`,
  $pipeName <: not r"^['\"]app[A-Z].*",
  register_diagnostic(span = $pipeName, message = "Pipe name must start with the configured prefix (default: 'app').", severity = "warn")
}
