language js

`@Component({ $args })` where {
  $args <: contains `selector: $sel`,
  $sel <: not r"^['\"]app-[a-z][a-z0-9]*(?:-[a-z0-9]+)*['\"]$",
  register_diagnostic(span = $sel, message = "Angular component selector must be kebab-case with the 'app-' prefix.", severity = "warn")
}
