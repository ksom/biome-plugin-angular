language js

`@Output() $name = $init` where {
  $name <: r"^on[A-Z].*",
  register_diagnostic(span = $name, message = "@Output() property names must not start with 'on' followed by an uppercase letter.")
}
