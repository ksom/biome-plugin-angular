language js

`@Output() $name = $init` where {
  $name <: r".*Change$",
  register_diagnostic(span = $name, message = "Prefer model() over the @Input() + @Output() xyzChange two-way binding pattern (Angular 17.2+).", severity = "warn")
}
